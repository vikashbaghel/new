<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background"
        android:drawable="@drawable/ic_empty_star" /> <!-- Use an empty star drawable for the background -->
    <item android:id="@android:id/secondaryProgress"
        android:drawable="@drawable/ic_empty_star" /> <!-- Use an empty star drawable for the secondary progress (if needed) -->
    <item android:id="@android:id/progress"
        android:drawable="@drawable/ic_filled_star" /><!-- Use a filled star drawable for the progress -->
</layer-list>